# IMPORTANT

* We use [Volta](https://volta.sh/) as NodeJS tool manager
* We use NPM as package manager

## Server (dev)
```
cd server
npm run dev
```

## Client (webpack, dev)
```
cd client
npm i
npm run start
```